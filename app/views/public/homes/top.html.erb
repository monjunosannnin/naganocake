<div class="container">
  <div class="row">
    <div class="col-md-3">  
      <div class="panel panel-default">
        <div class="panel-heading">
        　ジャンル検索
        </div>
        <% @genres.each do |genre| %>
          <div class="panel-body">
            <%= link_to genre.name, products_path(genre_id: genre.id) %>
          </div>
        <% end %>
        <div class="panel-footer">
        </div>
      </div>
    </div>
    <div class="col-md-9">
      <h4>ようこそ、NaganoCakeへ！</h4>
      <h4>このサイトは、ケーキ販売のECサイトになります。</h4>
      <h4>会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。
      </h4>
    <img src="/assets/cakes.jpg" alt="" width="650" height="450">
    <h3>オススメ商品</h3>
      <% @random.each do |product|%>
        <div class="col-xs-3">
          <div>
            <%#= attachment_image_tag product, :image, :fill, 100, 100%>
            <%= link_to product.name, product_path(product.id) %><br>
            ￥<%= (product.price*1.1).to_i %>
          </div>
        </div>
      <% end %>
      <div class="text-right">
        <%= link_to  "もっと見る＞", products_path, class: "cs-link" %>
      </div>
    </div>
  </div>
</div>