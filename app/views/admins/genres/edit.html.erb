<%= form_with model: @genre, url: admins_genre_path, id: "genre_form" do |f| %>
  <%= render "form", genre: @genre, f: f %>

  <div class="actions" id="app_button">
    <%= f.submit "変更を保存する" %>
  </div>
<% end %>